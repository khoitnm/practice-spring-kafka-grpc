server.port: ${port:8080}
kafka-global-producer:
  bootstrap-servers: localhost:9092

app:
  topic:
    example: topic_01

kafka:
  topics:
    topic_01:
      numPartitions: 3
      replicationFactor: 2
    topic_02:
      numPartitions: 4
      replicationFactor: 2
